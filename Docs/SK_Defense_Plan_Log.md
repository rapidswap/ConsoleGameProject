# SK Defense - 기획 및 개발 로그

이 문서는 `CraftEngine` 기반 스타크래프트 '성큰 디펜스' 스타일 게임(프로젝트명: SK Defense) 개발을 위한 기획 및 로그 문서입니다.

## 📌 기능별 구현 플랜 (Implementation Plan)

### 1. 카메라 시스템 및 줌 (WASD 및 확대/축소)
- **WASD 카메라 이동**: `Level` 또는 `GameManager`에 `CameraOffset` 변수를 추가하여 렌더링 시 오프셋을 적용.
- **맵 확대/축소**: Windows API `SetCurrentConsoleFontEx`를 이용해 콘솔 폰트 크기를 변경하여 줌 인/아웃 효과 구현.

### 2. 마우스 입력 및 터렛 설치 (2x2)
- **터렛 미리보기**: `Input::GetMousePosition()`을 가져와 마우스 커서 위치에 2x2 크기의 터렛 설치 가능 여부를 시각적으로 표시.
- **터렛 설치 및 타일 속성 변경**: 클릭 시 터렛 생성 및 맵 그리드(Grid)의 Walkable 상태 업데이트.

### 3. A* 경로 탐색 알고리즘
- **A* 길찾기 로직 구현**: 타일 맵 기반의 최단 경로 탐색 시스템.
- **동적 경로 재탐색**: 터렛이 설치되어 지형이 바뀔 때마다 맵 내 모든 적(Enemy)들의 경로를 갱신하여 터렛을 피해가도록 함.

### 4. 게임 루프 및 자원/웨이브 시스템
- **웨이브 매니저**: 일정 주기마다 적(Enemy) 생성. 웨이브가 올라갈수록 적의 체력/이동속도 증가.
- **재화(Gold) 시스템**: 적 처치 시 골드 획득. 골드를 소비하여 설치된 터렛을 클릭 시 업그레이드 가능.

### 5. 디버그 모드
- **A* 경로 시각화**: 적이 찾은 노드(경로)를 화면에 다른 색상(예: `*`)으로 그려서 시각적 확인.
- **치트키**: 골드 획득(G), 웨이브 스킵(F1) 기능 구현.

---

## 🛠️ 개발 로그 및 트러블슈팅 (Dev Log)

### 2026-08-21
- **진행 내용**: 
  - CraftEngine 분석 및 SK Defense 게임 개발 가능 여부 검토 완료.
  - 마우스 입력 처리, 화면 렌더링, A* 알고리즘 등 8가지 기능 플랜 작성.
  - 개발 내역이 유실되지 않도록 프로젝트 내 `Docs` 폴더에 통합 문서화.
- **트러블슈팅**: 
  - (해결) *화면 줌 인/아웃 기능:* 콘솔 화면 특성상 그래픽적 줌이 불가능한 한계가 있었으나, Win32 API의 `SetCurrentConsoleFontEx`를 사용하여 터미널 폰트 크기 자체를 변경하는 방식으로 우회 구현하기로 결정.

### 2026-08-24
- **진행 내용**: 
  - `Project_SKDefense` 프로젝트 세팅 및 `DefenseLevel` 메인 레벨로 연동.
  - 50x20 크기의 맵 구조(`SK_Defense_Map.txt`) 설계 및 Wall/Ground 액터 자동 배치 로직 구현.
  - WASD 키를 이용한 자유로운 맵 카메라 이동 구현.
  - 마우스 위치에 2x2 크기 터렛 미리보기(TT) 구현 및 왼쪽 클릭 시 터렛 설치 기능 1차 완료.
- **트러블슈팅**: 
  - (해결) *윈도우 터미널 마우스 실시간 추적 불가 현상:* 최신 터미널 환경에서 마우스를 클릭/드래그하지 않으면 `MOUSE_MOVED` 이벤트를 콘솔 버퍼에 넘겨주지 않는 고질적 문제가 발생. 이를 우회하기 위해 윈도우 OS의 물리적 마우스 픽셀 좌표(`GetCursorPos`)를 윈도우 핸들(`GetConsoleWindow()`) 기준으로 변환한 뒤, 화면 픽셀 비율(`GetClientRect`)로 직접 칸(Cell) 단위 위치를 계산하는 100% 안전한 커스텀 추적 함수(`GetRealMousePos()`)를 제작하여 완벽 동기화 달성.
  - (해결) *마우스 클릭 시 터렛 설치 위치 엇나감:* `GetAsyncKeyState`를 이용한 OS 틱과 엔진 콘솔 큐 간의 이벤트 처리 시차로 인해 좌표가 틀어지는 문제를 방지하기 위해 엔진 자체 이벤트 큐를 유지하되, 마우스 위치만 OS단에서 가져오는 방식으로 하이브리드 구조를 확립함.

### 2026-08-24 (오후)
- **진행 내용**: 
  - \AStar\ 길찾기 알고리즘 연동 및 몬스터(\Enemy\) 자동 이동 로직 추가.
  - 마우스 클릭으로 터렛(2x2) 설치 시 지형 데이터를 갱신(장애물 값 \2\ 부여)하고, 실시간으로 맵에 생존해 있는 몬스터들의 경로를 재탐색(\RecalculatePath\)하도록 동적 회피 기능 구현.
  - \EnemySpawner\ 생성 및 스폰 포인트(\S\ 지점) 데이터 동기화 완료.
  - **오브젝트 풀링(Object Pooling)** 적용: \maxPerWave(30)\ 만큼의 몬스터를 \BeginPlay\ 시점에 미리 메모리에 할당해 두고(\isActive=false\), 스폰 타이머에 맞춰 한 마리씩 재활용(\SetActive(true)\)하여 성능(메모리 할당/해제 오버헤드) 대폭 최적화.
  - **웨이브 상태 관리 및 타이머 UI**: \DefenseLevel\ 좌측 상단에 웨이브 대기 시간(mm:ss) 및 진행 상태(Wave In Progress)를 렌더링하도록 구현.
- **트러블슈팅**:
  - (해결) *A* 길찾기 알고리즘 구동 시 화면 완전 정지:* \AStar::FindPath\ 내부에 디버그를 위한 \DisplayGrid()\ 및 \Sleep(50)\ 코드가 남아있어 메인 스레드가 정지되는 문제가 발생. 해당 부분을 주석 처리하여 게임 루프 블로킹 현상 해결.
  - (해결) *몬스터가 S 위치에서 이동하지 않는 버그:* \DefenseLevel::Tick\을 오버라이딩하면서 부모 클래스의 \Level::Tick(deltaTime)\ 호출을 누락하여 맵에 소속된 액터들의 \Tick\ 루프가 돌지 않던 문제를 발견. 부모 \Tick\ 호출 코드를 추가하여 몬스터 등 액터 로직 정상화.
  - (해결) *새로 지은 터렛을 무시하고 뚫고 지나가는 문제:* A* 탐색 조건에 벽(\1\)만 체크되어 있던 것을 터렛(\2\)도 포함하도록 수정(\grid == 1 || grid == 2\). 또한 터렛 건설 시 기존 몬스터들의 구식 경로가 갱신되지 않는 문제를 해결하기 위해, 맵 갱신 즉시 루프를 돌며 모든 활성화된 몬스터의 \RecalculatePath()\ 함수를 강제로 호출하도록 조치함.


### 2026-08-25
- **진행 내용**:
  - \TurretBullet\ 액터 스크립트 작성 및 팩토리(vcxproj) 연동 완료.
  - 마우스 실시간 추적 기능과 윈도우 터미널(Windows Terminal)의 가상 콘솔(ConPTY) 아키텍처 간의 호환성 분석 및 트러블슈팅 완료.
- **트러블슈팅**:
  - (해결) *TurretBullet 내부에서 FindActor 호출 시 발생하는 문제:* 총알이 생성된 후 날 쏜 터렛을 \FindActor\로 찾으려 할 경우, 맵에 여러 터렛이 있으면 어떤 터렛인지 식별할 수 없는 논리적 오류가 발생함. 이를 해결하기 위해 총알이 생성될 때(\생성자\) 발사된 위치(\startPosition\)를 스스로 저장하도록 구조를 변경. 이후 \Tick\ 함수에서 현재 위치와 시작 위치의 거리를 실시간으로 계산하여, 사거리(\ulletRange\)를 벗어나면 스스로 파괴(\Destroy\)되도록 아주 가볍고 안정적인 로직으로 우회 구현 완료.
  - (해결) *윈도우 터미널에서 마우스가 추적되지 않는 현상:* 윈도우 터미널은 실제 창을 렌더링하지 않는 '가상 콘솔'로 동작하기 때문에, 물리 좌표를 변환하는 \GetRealMousePos()\의 \GetConsoleWindow()\ 및 \ScreenToClient()\ 함수가 정상 동작하지 않는 태생적 한계가 있음을 확인. 엔진 구조를 완전히 뜯어고치는 대신 기존의 **콘솔 호스트(cmd)** 환경을 표준으로 사용하는 것으로 결론을 도출함. 더불어 중복으로 적용되었던 빠른 편집 모드 비활성화 코드를 정리하여 엔진 호환성 유지.

