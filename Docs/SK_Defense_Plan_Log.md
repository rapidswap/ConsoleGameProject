# SK Defense - 기획 및 개발 로그

이 문서는 `CraftEngine` 기반 스타크래프트 '성큰 디펜스' 스타일 게임(프로젝트명: SK Defense) 개발을 위한 기획 및 로그 문서입니다.

## 📌 기능별 구현 플랜 (Implementation Plan)

### 1. 카메라 시스템 및 줌 (WASD 및 확대/축소)
- **WASD 카메라 이동**: `Level` 또는 `GameManager`에 `CameraOffset` 변수를 추가하여 렌더링 시 오프셋을 적용.
- **맵 확대/축소**: Windows API `SetCurrentConsoleFontEx`를 이용해 콘솔 폰트 크기를 변경하여 줌 인/아웃 효과 구현.

### 2. 마우스 입력 및 터렛 설치 (2x2)
- **터렛 미리보기**: `Input::GetMousePosition()`을 가져와 마우스 커서 위치에 2x2 크기의 터렛 설치 가능 여부를 시각적으로 표시.
- **터렛 설치 및 타일 속성 변경**: 클릭 시 터렛 생성 및 맵 그리드(Grid)의 Walkable 상태 업데이트.

### 3. A* 경로 탐색 알고리즘
- **A* 길찾기 로직 구현**: 타일 맵 기반의 최단 경로 탐색 시스템.
- **동적 경로 재탐색**: 터렛이 설치되어 지형이 바뀔 때마다 맵 내 모든 적(Enemy)들의 경로를 갱신하여 터렛을 피해가도록 함.

### 4. 게임 루프 및 자원/웨이브 시스템
- **웨이브 매니저**: 일정 주기마다 적(Enemy) 생성. 웨이브가 올라갈수록 적의 체력/이동속도 증가.
- **재화(Gold) 시스템**: 적 처치 시 골드 획득. 골드를 소비하여 설치된 터렛을 클릭 시 업그레이드 가능.

### 5. 디버그 모드
- **A* 경로 시각화**: 적이 찾은 노드(경로)를 화면에 다른 색상(예: `*`)으로 그려서 시각적 확인.
- **치트키**: 골드 획득(G), 웨이브 스킵(F1) 기능 구현.

---

## 🛠️ 개발 로그 및 트러블슈팅 (Dev Log)

### 2026-08-21
- **진행 내용**: 
  - CraftEngine 분석 및 SK Defense 게임 개발 가능 여부 검토 완료.
  - 마우스 입력 처리, 화면 렌더링, A* 알고리즘 등 8가지 기능 플랜 작성.
  - 개발 내역이 유실되지 않도록 프로젝트 내 `Docs` 폴더에 통합 문서화.
- **트러블슈팅**: 
  - (해결) *화면 줌 인/아웃 기능:* 콘솔 화면 특성상 그래픽적 줌이 불가능한 한계가 있었으나, Win32 API의 `SetCurrentConsoleFontEx`를 사용하여 터미널 폰트 크기 자체를 변경하는 방식으로 우회 구현하기로 결정.

### 2026-08-24
- **진행 내용**: 
  - `Project_SKDefense` 프로젝트 세팅 및 `DefenseLevel` 메인 레벨로 연동.
  - 50x20 크기의 맵 구조(`SK_Defense_Map.txt`) 설계 및 Wall/Ground 액터 자동 배치 로직 구현.
  - WASD 키를 이용한 자유로운 맵 카메라 이동 구현.
  - 마우스 위치에 2x2 크기 터렛 미리보기(TT) 구현 및 왼쪽 클릭 시 터렛 설치 기능 1차 완료.
- **트러블슈팅**: 
  - (해결) *윈도우 터미널 마우스 실시간 추적 불가 현상:* 최신 터미널 환경에서 마우스를 클릭/드래그하지 않으면 `MOUSE_MOVED` 이벤트를 콘솔 버퍼에 넘겨주지 않는 고질적 문제가 발생. 이를 우회하기 위해 윈도우 OS의 물리적 마우스 픽셀 좌표(`GetCursorPos`)를 윈도우 핸들(`GetConsoleWindow()`) 기준으로 변환한 뒤, 화면 픽셀 비율(`GetClientRect`)로 직접 칸(Cell) 단위 위치를 계산하는 100% 안전한 커스텀 추적 함수(`GetRealMousePos()`)를 제작하여 완벽 동기화 달성.
  - (해결) *마우스 클릭 시 터렛 설치 위치 엇나감:* `GetAsyncKeyState`를 이용한 OS 틱과 엔진 콘솔 큐 간의 이벤트 처리 시차로 인해 좌표가 틀어지는 문제를 방지하기 위해 엔진 자체 이벤트 큐를 유지하되, 마우스 위치만 OS단에서 가져오는 방식으로 하이브리드 구조를 확립함.

### 2026-08-24 (오후)
- **진행 내용**: 
  - \AStar\ 길찾기 알고리즘 연동 및 몬스터(\Enemy\) 자동 이동 로직 추가.
  - 마우스 클릭으로 터렛(2x2) 설치 시 지형 데이터를 갱신(장애물 값 \2\ 부여)하고, 실시간으로 맵에 생존해 있는 몬스터들의 경로를 재탐색(\RecalculatePath\)하도록 동적 회피 기능 구현.
  - \EnemySpawner\ 생성 및 스폰 포인트(\S\ 지점) 데이터 동기화 완료.
  - **오브젝트 풀링(Object Pooling)** 적용: \maxPerWave(30)\ 만큼의 몬스터를 \BeginPlay\ 시점에 미리 메모리에 할당해 두고(\isActive=false\), 스폰 타이머에 맞춰 한 마리씩 재활용(\SetActive(true)\)하여 성능(메모리 할당/해제 오버헤드) 대폭 최적화.
  - **웨이브 상태 관리 및 타이머 UI**: \DefenseLevel\ 좌측 상단에 웨이브 대기 시간(mm:ss) 및 진행 상태(Wave In Progress)를 렌더링하도록 구현.
- **트러블슈팅**:
  - (해결) *A* 길찾기 알고리즘 구동 시 화면 완전 정지:* \AStar::FindPath\ 내부에 디버그를 위한 \DisplayGrid()\ 및 \Sleep(50)\ 코드가 남아있어 메인 스레드가 정지되는 문제가 발생. 해당 부분을 주석 처리하여 게임 루프 블로킹 현상 해결.
  - (해결) *몬스터가 S 위치에서 이동하지 않는 버그:* \DefenseLevel::Tick\을 오버라이딩하면서 부모 클래스의 \Level::Tick(deltaTime)\ 호출을 누락하여 맵에 소속된 액터들의 \Tick\ 루프가 돌지 않던 문제를 발견. 부모 \Tick\ 호출 코드를 추가하여 몬스터 등 액터 로직 정상화.
  - (해결) *새로 지은 터렛을 무시하고 뚫고 지나가는 문제:* A* 탐색 조건에 벽(\1\)만 체크되어 있던 것을 터렛(\2\)도 포함하도록 수정(\grid == 1 || grid == 2\). 또한 터렛 건설 시 기존 몬스터들의 구식 경로가 갱신되지 않는 문제를 해결하기 위해, 맵 갱신 즉시 루프를 돌며 모든 활성화된 몬스터의 \RecalculatePath()\ 함수를 강제로 호출하도록 조치함.


### 2026-08-25
- **진행 내용**:
  - \TurretBullet\ 액터 스크립트 작성 및 팩토리(vcxproj) 연동 완료.
  - 마우스 실시간 추적 기능과 윈도우 터미널(Windows Terminal)의 가상 콘솔(ConPTY) 아키텍처 간의 호환성 분석 및 트러블슈팅 완료.
- **트러블슈팅**:
  - (해결) *TurretBullet 내부에서 FindActor 호출 시 발생하는 문제:* 총알이 생성된 후 날 쏜 터렛을 \FindActor\로 찾으려 할 경우, 맵에 여러 터렛이 있으면 어떤 터렛인지 식별할 수 없는 논리적 오류가 발생함. 이를 해결하기 위해 총알이 생성될 때(\생성자\) 발사된 위치(\startPosition\)를 스스로 저장하도록 구조를 변경. 이후 \Tick\ 함수에서 현재 위치와 시작 위치의 거리를 실시간으로 계산하여, 사거리(\ulletRange\)를 벗어나면 스스로 파괴(\Destroy\)되도록 아주 가볍고 안정적인 로직으로 우회 구현 완료.
  - (해결) *윈도우 터미널에서 마우스가 추적되지 않는 현상:* 윈도우 터미널은 실제 창을 렌더링하지 않는 '가상 콘솔'로 동작하기 때문에, 물리 좌표를 변환하는 \GetRealMousePos()\의 \GetConsoleWindow()\ 및 \ScreenToClient()\ 함수가 정상 동작하지 않는 태생적 한계가 있음을 확인. 엔진 구조를 완전히 뜯어고치는 대신 기존의 **콘솔 호스트(cmd)** 환경을 표준으로 사용하는 것으로 결론을 도출함. 더불어 중복으로 적용되었던 빠른 편집 모드 비활성화 코드를 정리하여 엔진 호환성 유지.


### 2026-08-26
- **트러블슈팅**:
  - (해결) *오브젝트 풀링 재활용 버그:* 웨이브가 넘어갈 때 몬스터가 다시 나오지 않는 현상 발견. 몬스터가 체력을 다해 죽거나 아지트에 도달했을 때 메모리에서 강제로 \Destroy()\ 해버려, 30개의 크기로 고정된 풀(Pool)에서 더 이상 빼내 쓸 몬스터가 없어지는 문제였음. 이를 해결하기 위해 파괴 대신 \SetActive(false)\로 상태만 비활성화하여 풀에 반납하도록 수정.
  - (해결) *재활용 시 체력 초기화 로직 보완:* 풀에서 깨워낼 때 죽었던 몬스터의 체력이 여전히 0 이하로 남아있어 스폰 즉시 다시 죽어버리는 현상을 방지하고자, 스포너에서 \SetActive(true)\를 호출하기 직전에 현재 웨이브 수 비례 체력(\currentWave * 5\)으로 리셋해주도록 로직 보완 완벽 해결.


### 2026-08-27
- **진행 내용**:
  - **멀티 씬(레벨) 관리자 세팅**: `MainMenuLevel`, `EscMenu`, `GameOverLevel`, `GameClearLevel` 추가 및 연동. ESC 키와 메뉴 간의 씬 전환 흐름(기승전결)을 완성함.
  - **골드(Gold) 시스템 연동**: 디펜스 레벨에 재화 관리 시스템 도입. 시작 시 기본 골드(100) 부여, 몬스터 처치 시 골드 획득(+10), 터렛 설치 시 골드 차감(-50) 로직 추가. 골드 부족 시 설치 불가 처리(빨간색 미리보기) 및 UI 표시 기능 완료. 디버그용 치트키(G: 골드 1000 증가) 추가.
  - **승리/패배(Game Clear/Over) 조건 구현**: 
    - 아지트 체력이 0 이하가 되면 즉시 `GameOverLevel`로 전환되도록 구현.
    - 마지막 웨이브(3)의 모든 몬스터 스폰이 끝난 후, 맵 상의 활성화된 몬스터가 한 마리도 없을 때(`IsActive() == false`) `GameClearLevel`로 전환되도록 전멸 체크 로직 구현.
  - 디버그용 치트키(F1: 웨이브 대기시간 즉시 스킵) 추가 완료.

- **트러블슈팅**:
  - (해결) *구조체 중복 정의(Redefinition) 오류*: `EscMenu.h`와 `GameOverLevel.h`에서 각각 `MenuItem` 구조체를 정의하여 `Game.cpp`에서 인클루드 시 충돌이 발생하는 문제를 발견. `MenuItem.h`라는 공용 헤더 파일을 분리 생성하여 양쪽에서 인클루드 하도록 구조를 개선함.
  - (해결) *ESC 메뉴 진입 시 게임 크래시 현상*: `Game.cpp`의 `levelList` 배열에는 인덱스가 1번까지밖에 없으나, 열거형(enum `State`)에서 `ESCMENU`가 2번, `GAMEOVER`가 4번 등으로 할당되어 있어 발생한 Out of Bounds 메모리 참조 오류. `levelList`에 씬들을 정확한 순서로 `emplace_back`하고 Enum의 번호와 배열 인덱스를 1:1로 강제 매칭시켜 크래시 해결.
  - (해결) *웨이브 강제 스킵 시 클리어 불가 버그*: F1 키로 마지막 웨이브까지 스킵 후 몬스터를 전멸시켜도 게임 클리어가 되지 않던 현상 발견. 원인은 스포너가 3웨이브 스폰을 마치자마자 내부 `currentWave` 변수를 4로 올려버려서 `currentWave == 3` 조건문에 영영 진입하지 못하는 논리 오류였음. 이를 `currentWave > 3` 상태일 때 전멸을 체크하도록 수정하여 완벽 해결함.
  - (해결) *게임 오버 후 다시 시작(Restart) 버그 위험성 방지*: 게임 오버 후 단순히 `ToggleMenu`로 1번 씬으로 돌아가면 맵 파괴 상태나 몬스터 잔해가 그대로 남을 수 있다는 고민(이슈)이 있었음. 이를 해결하기 위해 `RestartDefenseLevel()`이라는 전용 초기화 함수를 만들어, 재시작 시 `levelList[1]`에 완전 새로운 `DefenseLevel` 객체를 덮어씌워 맵과 상태를 초기화하는 방식으로 해결.

- **나의 고민 (User Thoughts)**:
  - 디펜스 게임에서 유저가 맵을 마음대로 터렛으로 막을 수 있게 두면 몬스터가 영원히 아지트로 못 가는 "완전 길막(Maze block)" 꼼수가 발생할 수 있다는 점을 인지함.
  - 어떻게 하면 직관적으로 길막을 방지할 수 있을까 고민하던 중, 이미 만들어둔 A* 길찾기 알고리즘을 역이용하여 "가상의 유령 몬스터를 보내보고, 길이 없으면 터렛을 설치하지 못하게 막는" 우회 시뮬레이션 방식을 기획하게 됨. 다음 단계로 구현 예정!


### 2026-08-27 (오후)
- **진행 내용**:
  - **터렛 완전 길막(Maze block) 방지 시스템 완성**:
    - `DefenseLevel::CanBuildTurret`에서 터렛 임시 배치 후 A* 길찾기 시뮬레이션을 수행하여, 스폰 지점(S)부터 아지트(D)까지 경로가 없거나 맵에 살아있는 몬스터가 갇히게 되는 경우 설치를 차단(프리뷰 빨간색 표시 및 클릭 무시)하도록 구현 완료.
  - **터렛 철거(판매) 시스템 구현**:
    - 마우스 우클릭 시 해당 위치의 터렛을 철거하고, 타일을 빈 땅(0)으로 복구하며 성급에 비례한 골드 환불(1성 25G, 2성 75G, 3성 225G) 및 몬스터 경로 동적 재계산 처리.
  - **랜덤 터렛 & 3단 자동 합성(3-Merge Auto Upgrade) 시스템 구현**:
    - 터렛 설치 시 3가지 속성(화염 `FF`, 얼음 `II`, 전기 `TT`) 중 하나가 무작위로 생성됨.
    - 동일한 속성과 성급(1성/2성)의 터렛이 맵에 3개 모이면, **가장 먼저 지어진 1번 터렛이 다음 성급(2성 `+`, 3성 `*`)으로 자동 승급**하고 나머지 2개 터렛은 파괴되며 타일이 빈 땅으로 복구됨.
    - 연쇄 합성 지원: 1성 3개가 합쳐져 2성이 되었을 때, 마침 2성이 3개가 되면 즉시 3성으로 연쇄 승급하는 do-while 루프 검사 구조 확립.

- **트러블슈팅 & 고민 해결**:
  - (해결) *엔진 RTTI 함수명 충돌*: 터렛 종류를 반환하기 위해 `GetType()` 함수를 선언했으나, 엔진 매크로 `TYPE_DECLARATIONS`가 내부적으로 생성하는 RTTI 함수 `size_t GetType()`과 이름 및 시그니처가 충돌하는 컴파일 에러 발생. 함수명을 `GetTurretType()`으로 변경하여 충돌 해결.
  - (해결) *신규 생성 액터 리스트 탐색 누락 방지*: 터렛 설치 직후 프레임에서는 새로 생성된 터렛이 `actorList`가 아닌 엔진의 대기 목록(`addRequestedActorList`)에 머무르고 있어 `FindActors`로 즉시 검색되지 않는 구조적 특성 발견. `CheckTurretMerge()` 함수에서 `actorList`와 `addRequestedActorList`를 모두 수집하여 합성 검사를 수행함으로써 설치 즉시 완벽하게 3단 합성이 발동되도록 동기화 완료.
  - (해결) *터렛 희생 시 맵 타일 복구*: 합성을 위해 2개의 터렛이 사라질 때 단순히 `Destroy()`만 하면 `mapGrid`에 터렛 장애물 번호(2)가 영구히 남아 몬스터가 못 지나가는 유령 벽 버그가 생길 수 있음을 방지하고자, 파괴 직전 해당 터렛들의 4칸 타일을 `mapGrid`에서 0(빈 땅)으로 완벽하게 리셋 후 `RecalculatePath()`를 호출하도록 조치.

### 2026-08-28 (Map, UI 개편 및 도박 슬롯머신 애니메이션 구현)
- **진행 내용**:
  - **맵 디자인 변경**: 유저가 자유롭게 길막(Mazing)을 설계할 수 있도록 50x20 크기의 2x2 그리드 기반 광장형(Open Field) 맵으로 재설계. 4개의 앵커(기둥) 배치.
  - **인터페이스(UI) 전면 개편**: 기존 맵 중앙에 겹쳐서 출력되던 텍스트들을 화면 우측 여백(X=65~115)에 '전용 UI 패널' 형태로 분리 배치. 하단 컨트롤 패널은 시각적 인지가 빠른 다중 라인(`Multi-line`) 버튼 레이아웃으로 변경.
# SK Defense - 기획 및 개발 로그

이 문서는 `CraftEngine` 기반 스타크래프트 '성큰 디펜스' 스타일 게임(프로젝트명: SK Defense) 개발을 위한 기획 및 로그 문서입니다.

## 📌 기능별 구현 플랜 (Implementation Plan)

### 1. 카메라 시스템 및 줌 (WASD 및 확대/축소)
- **WASD 카메라 이동**: `Level` 또는 `GameManager`에 `CameraOffset` 변수를 추가하여 렌더링 시 오프셋을 적용.
- **맵 확대/축소**: Windows API `SetCurrentConsoleFontEx`를 이용해 콘솔 폰트 크기를 변경하여 줌 인/아웃 효과 구현.

### 2. 마우스 입력 및 터렛 설치 (2x2)
- **터렛 미리보기**: `Input::GetMousePosition()`을 가져와 마우스 커서 위치에 2x2 크기의 터렛 설치 가능 여부를 시각적으로 표시.
- **터렛 설치 및 타일 속성 변경**: 클릭 시 터렛 생성 및 맵 그리드(Grid)의 Walkable 상태 업데이트.

### 3. A* 경로 탐색 알고리즘
- **A* 길찾기 로직 구현**: 타일 맵 기반의 최단 경로 탐색 시스템.
- **동적 경로 재탐색**: 터렛이 설치되어 지형이 바뀔 때마다 맵 내 모든 적(Enemy)들의 경로를 갱신하여 터렛을 피해가도록 함.

### 4. 게임 루프 및 자원/웨이브 시스템
- **웨이브 매니저**: 일정 주기마다 적(Enemy) 생성. 웨이브가 올라갈수록 적의 체력/이동속도 증가.
- **재화(Gold) 시스템**: 적 처치 시 골드 획득. 골드를 소비하여 설치된 터렛을 클릭 시 업그레이드 가능.

### 5. 디버그 모드
- **A* 경로 시각화**: 적이 찾은 노드(경로)를 화면에 다른 색상(예: `*`)으로 그려서 시각적 확인.
- **치트키**: 골드 획득(G), 웨이브 스킵(F1) 기능 구현.

---

## 🛠️ 개발 로그 및 트러블슈팅 (Dev Log)

### 2026-08-21
- **진행 내용**: 
  - CraftEngine 분석 및 SK Defense 게임 개발 가능 여부 검토 완료.
  - 마우스 입력 처리, 화면 렌더링, A* 알고리즘 등 8가지 기능 플랜 작성.
  - 개발 내역이 유실되지 않도록 프로젝트 내 `Docs` 폴더에 통합 문서화.
- **트러블슈팅**: 
  - (해결) *화면 줌 인/아웃 기능:* 콘솔 화면 특성상 그래픽적 줌이 불가능한 한계가 있었으나, Win32 API의 `SetCurrentConsoleFontEx`를 사용하여 터미널 폰트 크기 자체를 변경하는 방식으로 우회 구현하기로 결정.

### 2026-08-24
- **진행 내용**: 
  - `Project_SKDefense` 프로젝트 세팅 및 `DefenseLevel` 메인 레벨로 연동.
  - 50x20 크기의 맵 구조(`SK_Defense_Map.txt`) 설계 및 Wall/Ground 액터 자동 배치 로직 구현.
  - WASD 키를 이용한 자유로운 맵 카메라 이동 구현.
  - 마우스 위치에 2x2 크기 터렛 미리보기(TT) 구현 및 왼쪽 클릭 시 터렛 설치 기능 1차 완료.
- **트러블슈팅**: 
  - (해결) *윈도우 터미널 마우스 실시간 추적 불가 현상:* 최신 터미널 환경에서 마우스를 클릭/드래그하지 않으면 `MOUSE_MOVED` 이벤트를 콘솔 버퍼에 넘겨주지 않는 고질적 문제가 발생. 이를 우회하기 위해 윈도우 OS의 물리적 마우스 픽셀 좌표(`GetCursorPos`)를 윈도우 핸들(`GetConsoleWindow()`) 기준으로 변환한 뒤, 화면 픽셀 비율(`GetClientRect`)로 직접 칸(Cell) 단위 위치를 계산하는 100% 안전한 커스텀 추적 함수(`GetRealMousePos()`)를 제작하여 완벽 동기화 달성.
  - (해결) *마우스 클릭 시 터렛 설치 위치 엇나감:* `GetAsyncKeyState`를 이용한 OS 틱과 엔진 콘솔 큐 간의 이벤트 처리 시차로 인해 좌표가 틀어지는 문제를 방지하기 위해 엔진 자체 이벤트 큐를 유지하되, 마우스 위치만 OS단에서 가져오는 방식으로 하이브리드 구조를 확립함.

### 2026-08-24 (오후)
- **진행 내용**: 
  - \AStar\ 길찾기 알고리즘 연동 및 몬스터(\Enemy\) 자동 이동 로직 추가.
  - 마우스 클릭으로 터렛(2x2) 설치 시 지형 데이터를 갱신(장애물 값 \2\ 부여)하고, 실시간으로 맵에 생존해 있는 몬스터들의 경로를 재탐색(\RecalculatePath\)하도록 동적 회피 기능 구현.
  - \EnemySpawner\ 생성 및 스폰 포인트(\S\ 지점) 데이터 동기화 완료.
  - **오브젝트 풀링(Object Pooling)** 적용: \maxPerWave(30)\ 만큼의 몬스터를 \BeginPlay\ 시점에 미리 메모리에 할당해 두고(\isActive=false\), 스폰 타이머에 맞춰 한 마리씩 재활용(\SetActive(true)\)하여 성능(메모리 할당/해제 오버헤드) 대폭 최적화.
  - **웨이브 상태 관리 및 타이머 UI**: \DefenseLevel\ 좌측 상단에 웨이브 대기 시간(mm:ss) 및 진행 상태(Wave In Progress)를 렌더링하도록 구현.
- **트러블슈팅**:
  - (해결) *A* 길찾기 알고리즘 구동 시 화면 완전 정지:* \AStar::FindPath\ 내부에 디버그를 위한 \DisplayGrid()\ 및 \Sleep(50)\ 코드가 남아있어 메인 스레드가 정지되는 문제가 발생. 해당 부분을 주석 처리하여 게임 루프 블로킹 현상 해결.
  - (해결) *몬스터가 S 위치에서 이동하지 않는 버그:* \DefenseLevel::Tick\을 오버라이딩하면서 부모 클래스의 \Level::Tick(deltaTime)\ 호출을 누락하여 맵에 소속된 액터들의 \Tick\ 루프가 돌지 않던 문제를 발견. 부모 \Tick\ 호출 코드를 추가하여 몬스터 등 액터 로직 정상화.
  - (해결) *새로 지은 터렛을 무시하고 뚫고 지나가는 문제:* A* 탐색 조건에 벽(\1\)만 체크되어 있던 것을 터렛(\2\)도 포함하도록 수정(\grid == 1 || grid == 2\). 또한 터렛 건설 시 기존 몬스터들의 구식 경로가 갱신되지 않는 문제를 해결하기 위해, 맵 갱신 즉시 루프를 돌며 모든 활성화된 몬스터의 \RecalculatePath()\ 함수를 강제로 호출하도록 조치함.


### 2026-08-25
- **진행 내용**:
  - \TurretBullet\ 액터 스크립트 작성 및 팩토리(vcxproj) 연동 완료.
  - 마우스 실시간 추적 기능과 윈도우 터미널(Windows Terminal)의 가상 콘솔(ConPTY) 아키텍처 간의 호환성 분석 및 트러블슈팅 완료.
- **트러블슈팅**:
  - (해결) *TurretBullet 내부에서 FindActor 호출 시 발생하는 문제:* 총알이 생성된 후 날 쏜 터렛을 \FindActor\로 찾으려 할 경우, 맵에 여러 터렛이 있으면 어떤 터렛인지 식별할 수 없는 논리적 오류가 발생함. 이를 해결하기 위해 총알이 생성될 때(\생성자\) 발사된 위치(\startPosition\)를 스스로 저장하도록 구조를 변경. 이후 \Tick\ 함수에서 현재 위치와 시작 위치의 거리를 실시간으로 계산하여, 사거리(\ ulletRange\)를 벗어나면 스스로 파괴(\Destroy\)되도록 아주 가볍고 안정적인 로직으로 우회 구현 완료.
  - (해결) *윈도우 터미널에서 마우스가 추적되지 않는 현상:* 윈도우 터미널은 실제 창을 렌더링하지 않는 '가상 콘솔'로 동작하기 때문에, 물리 좌표를 변환하는 \GetRealMousePos()\의 \GetConsoleWindow()\ 및 \ScreenToClient()\ 함수가 정상 동작하지 않는 태생적 한계가 있음을 확인. 엔진 구조를 완전히 뜯어고치는 대신 기존의 **콘솔 호스트(cmd)** 환경을 표준으로 사용하는 것으로 결론을 도출함. 더불어 중복으로 적용되었던 빠른 편집 모드 비활성화 코드를 정리하여 엔진 호환성 유지.


### 2026-08-26
- **트러블슈팅**:
  - (해결) *오브젝트 풀링 재활용 버그:* 웨이브가 넘어갈 때 몬스터가 다시 나오지 않는 현상 발견. 몬스터가 체력을 다해 죽거나 아지트에 도달했을 때 메모리에서 강제로 \Destroy()\ 해버려, 30개의 크기로 고정된 풀(Pool)에서 더 이상 빼내 쓸 몬스터가 없어지는 문제였음. 이를 해결하기 위해 파괴 대신 \SetActive(false)\로 상태만 비활성화하여 풀에 반납하도록 수정.
  - (해결) *재활용 시 체력 초기화 로직 보완:* 풀에서 깨워낼 때 죽었던 몬스터의 체력이 여전히 0 이하로 남아있어 스폰 즉시 다시 죽어버리는 현상을 방지하고자, 스포너에서 \SetActive(true)\를 호출하기 직전에 현재 웨이브 수 비례 체력(\currentWave * 5\)으로 리셋해주도록 로직 보완 완벽 해결.


### 2026-08-27
- **진행 내용**:
  - **멀티 씬(레벨) 관리자 세팅**: `MainMenuLevel`, `EscMenu`, `GameOverLevel`, `GameClearLevel` 추가 및 연동. ESC 키와 메뉴 간의 씬 전환 흐름(기승전결)을 완성함.
  - **골드(Gold) 시스템 연동**: 디펜스 레벨에 재화 관리 시스템 도입. 시작 시 기본 골드(100) 부여, 몬스터 처치 시 골드 획득(+10), 터렛 설치 시 골드 차감(-50) 로직 추가. 골드 부족 시 설치 불가 처리(빨간색 미리보기) 및 UI 표시 기능 완료. 디버그용 치트키(G: 골드 1000 증가) 추가.
  - **승리/패배(Game Clear/Over) 조건 구현**: 
    - 아지트 체력이 0 이하가 되면 즉시 `GameOverLevel`로 전환되도록 구현.
    - 마지막 웨이브(3)의 모든 몬스터 스폰이 끝난 후, 맵 상의 활성화된 몬스터가 한 마리도 없을 때(`IsActive() == false`) `GameClearLevel`로 전환되도록 전멸 체크 로직 구현.
  - 디버그용 치트키(F1: 웨이브 대기시간 즉시 스킵) 추가 완료.

- **트러블슈팅**:
  - (해결) *구조체 중복 정의(Redefinition) 오류*: `EscMenu.h`와 `GameOverLevel.h`에서 각각 `MenuItem` 구조체를 정의하여 `Game.cpp`에서 인클루드 시 충돌이 발생하는 문제를 발견. `MenuItem.h`라는 공용 헤더 파일을 분리 생성하여 양쪽에서 인클루드 하도록 구조를 개선함.
  - (해결) *ESC 메뉴 진입 시 게임 크래시 현상*: `Game.cpp`의 `levelList` 배열에는 인덱스가 1번까지밖에 없으나, 열거형(enum `State`)에서 `ESCMENU`가 2번, `GAMEOVER`가 4번 등으로 할당되어 있어 발생한 Out of Bounds 메모리 참조 오류. `levelList`에 씬들을 정확한 순서로 `emplace_back`하고 Enum의 번호와 배열 인덱스를 1:1로 강제 매칭시켜 크래시 해결.
  - (해결) *웨이브 강제 스킵 시 클리어 불가 버그*: F1 키로 마지막 웨이브까지 스킵 후 몬스터를 전멸시켜도 게임 클리어가 되지 않던 현상 발견. 원인은 스포너가 3웨이브 스폰을 마치자마자 내부 `currentWave` 변수를 4로 올려버려서 `currentWave == 3` 조건문에 영영 진입하지 못하는 논리 오류였음. 이를 `currentWave > 3` 상태일 때 전멸을 체크하도록 수정하여 완벽 해결함.
  - (해결) *게임 오버 후 다시 시작(Restart) 버그 위험성 방지*: 게임 오버 후 단순히 `ToggleMenu`로 1번 씬으로 돌아가면 맵 파괴 상태나 몬스터 잔해가 그대로 남을 수 있다는 고민(이슈)이 있었음. 이를 해결하기 위해 `RestartDefenseLevel()`이라는 전용 초기화 함수를 만들어, 재시작 시 `levelList[1]`에 완전 새로운 `DefenseLevel` 객체를 덮어씌워 맵과 상태를 초기화하는 방식으로 해결.

- **나의 고민 (User Thoughts)**:
  - 디펜스 게임에서 유저가 맵을 마음대로 터렛으로 막을 수 있게 두면 몬스터가 영원히 아지트로 못 가는 "완전 길막(Maze block)" 꼼수가 발생할 수 있다는 점을 인지함.
  - 어떻게 하면 직관적으로 길막을 방지할 수 있을까 고민하던 중, 이미 만들어둔 A* 길찾기 알고리즘을 역이용하여 "가상의 유령 몬스터를 보내보고, 길이 없으면 터렛을 설치하지 못하게 막는" 우회 시뮬레이션 방식을 기획하게 됨. 다음 단계로 구현 예정!


### 2026-08-27 (오후)
- **진행 내용**:
  - **터렛 완전 길막(Maze block) 방지 시스템 완성**:
    - `DefenseLevel::CanBuildTurret`에서 터렛 임시 배치 후 A* 길찾기 시뮬레이션을 수행하여, 스폰 지점(S)부터 아지트(D)까지 경로가 없거나 맵에 살아있는 몬스터가 갇히게 되는 경우 설치를 차단(프리뷰 빨간색 표시 및 클릭 무시)하도록 구현 완료.
  - **터렛 철거(판매) 시스템 구현**:
    - 마우스 우클릭 시 해당 위치의 터렛을 철거하고, 타일을 빈 땅(0)으로 복구하며 성급에 비례한 골드 환불(1성 25G, 2성 75G, 3성 225G) 및 몬스터 경로 동적 재계산 처리.
  - **랜덤 터렛 & 3단 자동 합성(3-Merge Auto Upgrade) 시스템 구현**:
    - 터렛 설치 시 3가지 속성(화염 `FF`, 얼음 `II`, 전기 `TT`) 중 하나가 무작위로 생성됨.
    - 동일한 속성과 성급(1성/2성)의 터렛이 맵에 3개 모이면, **가장 먼저 지어진 1번 터렛이 다음 성급(2성 `+`, 3성 `*`)으로 자동 승급**하고 나머지 2개 터렛은 파괴되며 타일이 빈 땅으로 복구됨.
    - 연쇄 합성 지원: 1성 3개가 합쳐져 2성이 되었을 때, 마침 2성이 3개가 되면 즉시 3성으로 연쇄 승급하는 do-while 루프 검사 구조 확립.

- **트러블슈팅 & 고민 해결**:
  - (해결) *엔진 RTTI 함수명 충돌*: 터렛 종류를 반환하기 위해 `GetType()` 함수를 선언했으나, 엔진 매크로 `TYPE_DECLARATIONS`가 내부적으로 생성하는 RTTI 함수 `size_t GetType()`과 이름 및 시그니처가 충돌하는 컴파일 에러 발생. 함수명을 `GetTurretType()`으로 변경하여 충돌 해결.
  - (해결) *신규 생성 액터 리스트 탐색 누락 방지*: 터렛 설치 직후 프레임에서는 새로 생성된 터렛이 `actorList`가 아닌 엔진의 대기 목록(`addRequestedActorList`)에 머무르고 있어 `FindActors`로 즉시 검색되지 않는 구조적 특성 발견. `CheckTurretMerge()` 함수에서 `actorList`와 `addRequestedActorList`를 모두 수집하여 합성 검사를 수행함으로써 설치 즉시 완벽하게 3단 합성이 발동되도록 동기화 완료.
  - (해결) *터렛 희생 시 맵 타일 복구*: 합성을 위해 2개의 터렛이 사라질 때 단순히 `Destroy()`만 하면 `mapGrid`에 터렛 장애물 번호(2)가 영구히 남아 몬스터가 못 지나가는 유령 벽 버그가 생길 수 있음을 방지하고자, 파괴 직전 해당 터렛들의 4칸 타일을 `mapGrid`에서 0(빈 땅)으로 완벽하게 리셋 후 `RecalculatePath()`를 호출하도록 조치.

### 2026-08-28 (Map, UI 개편 및 도박 슬롯머신 애니메이션 구현)
- **진행 내용**:
  - **맵 디자인 변경**: 유저가 자유롭게 길막(Mazing)을 설계할 수 있도록 50x20 크기의 2x2 그리드 기반 광장형(Open Field) 맵으로 재설계. 4개의 앵커(기둥) 배치.
  - **인터페이스(UI) 전면 개편**: 기존 맵 중앙에 겹쳐서 출력되던 텍스트들을 화면 우측 여백(X=65~115)에 '전용 UI 패널' 형태로 분리 배치. 하단 컨트롤 패널은 시각적 인지가 빠른 다중 라인(`Multi-line`) 버튼 레이아웃으로 변경.
  - **Tick 함수 리팩토링(Refactoring)**: 점점 거대해지는 `DefenseLevel::Tick` 함수를 `HandleCameraInput`, `HandleMouseInput`, `HandleUIInput`으로 기능 단위로 분리하여 유지보수성 대폭 향상.
  - **랜덤 기능(Upgrade/Gamble) 구현**: 상태 머신(State Machine) 패턴을 적용하여, 슬롯머신(Gamble) 애니메이션이 재생되는 5초 동안 본 게임의 Update(`Level::Tick`)를 스킵(일시정지)하고 중앙 팝업을 띄우도록 연출.

- **트러블슈팅**:
  - **(해결) 카메라 좌표계 오차로 인한 맵-UI 오버랩 버그**: 카메라 초기값이 맵의 정중앙으로 잡혀 있어 맵 우측 절반이 UI 뒤에 숨겨지고 마우스 클릭 좌표가 어긋나는 문제 발생. 카메라의 초기 위치를 화면 절반(`screenWidth / 2`)으로 재정렬하여 맵(X=0~49)과 UI(X=65~115)를 시각적으로 분리함. 또한 UI 뒷면에 맵 오브젝트들이 관통되어 보이는 현상을 막기 위해 렌더 순위(90)에 검정색 공백 배열(Black Cover)을 배치하여 배경을 차단함.
  - **(해결) ASCII 기반 콘솔 엔진에서의 멀티바이트(한글) 깨짐 현상**: `CraftEngine`은 글자를 1바이트(`CHAR_INFO`)로 계산하여 렌더링하지만 한글은 2~3바이트이므로 문자가 깨지고 배열이 틀어지는 증상 발생. 엔진 코어의 유니코드(`wchar_t`) 전환 대공사 대신, 비용 대비 효율이 높은 영문(English) 텍스트 교체 방식을 채택하여 빠르고 세련되게 버그를 픽스함.
  - (해결) 팝업 숫자가 증발하는 댕글링 포인터(Dangling Pointer) 버그**: 주사위 숫자를 그릴 때 `std::to_string(num).c_str()`과 같이 임시 문자열 객체의 메모리 주소를 바로 참조하여, 렌더링 직전에 객체가 소멸하면서 글자가 그려지지 않는 전형적인 C++ 메모리 함정에 빠짐. `std::string` 로컬 변수를 선언해 데이터를 안전하게 연장 보관한 후 참조하도록 수정하여 해결함.
  - **(참고) 모듈러 편중(Modulo Bias) 현상 리뷰**: `rand() % 3` 등을 사용할 때 발생하는 미세한 확률적 편중성(약 0.003%)에 대해 검토했으나, 캐주얼 디펜스 게임에서 유저 체감이 불가능한 수준이므로 C++11 `<random>` 라이브러리의 오버헤드 대신 기본 알고리즘을 유지하기로 기획 방향 확정.

### 2026-08-31 (UI 팝업 구현, 디버그 모드 시각화, 다중 스폰 난이도 스케일링)
- **진행 내용**:
  - **GameInfo UI 연동**: `Info/GameInfo.txt` 파일을 읽어와 F12 키 입력 시 화면 중앙에 게임 규칙과 조작법을 보여주는 팝업창 연출 구현.
  - **F3 디버그 시각화 모드 (A* 길찾기 애니메이션)**: 
    - F3 키를 누를 경우, 몬스터의 스폰 속도를 10초로 지연시키고 터렛이 공격하지 않도록 설정하여 유저가 여유롭게 길찾기 알고리즘을 관찰할 수 있는 디버그 환경 조성.
    - 몬스터의 발밑부터 아지트까지 이어지는 A* 경로 좌표 리스트(`GetPath`)를 순회하며, 점진적으로 그려지는 선형 애니메이션(Shooting Star effect)을 `Renderer`에 추가함.
  - **다중 입구 개방 (Multi-Spawn Level Design)**:
    - 게임의 긴장감을 높이기 위해 웨이브 진행도에 따라 새로운 몬스터 출입구가 뚫리는 스케일링 로직 구현.
    - `DefenseLevel`의 단일 `spawnPoint` 변수를 `std::vector<Craft::Vector2> spawnPoints` 배열로 리팩토링.
    - `EnemySpawner`에서 웨이브 4, 웨이브 7 도달 시 활성화되는 입구 개수(`activeSpawns`)를 늘려 랜덤한 입구에서 몬스터가 쏟아져 나오도록 다이내믹한 난이도 조정 시스템 완성.

- **트러블슈팅**:
  - **(해결) GameInfo 팝업 렌더링 프레임 저하 및 문자열 깨짐**: 파일을 매 틱(Tick)마다 열고 한 글자씩 렌더러에 넘겨 부하가 발생하던 기존 코드를, 문자열 전체를 한 줄(`\n`) 단위로 파싱하여 한 번에 `Submit`하도록 리팩토링하여 최적화 달성. 추가로 한글 출력 시 엔진 한계로 렌더링이 깨지는 문제를 영문(English) 기반 압축 텍스트 팝업으로 재설계하여 깔끔하게 해결.
  - **(해결) 다중 스폰 지점 변경 후 A* 완전 길막(Maze Block) 방지 로직 오류**: 기존 터렛 설치 가능 여부를 체크하는 `CanBuildTurret()` 함수가 단일 `spawnPoint` 변수를 참조하던 중, 배열(`spawnPoints`)로 리팩토링되면서 스폰 지점 길찾기 무결성 검증이 누락되는 치명적 버그 발견(컴파일 단계에서 삭제된 변수 참조로 오류 발생).
  - **(해결 과정)**: 모든 스폰 지점(`spawnPoints` 반복문)에서 아지트(D)까지의 경로를 각각 개별적으로 시뮬레이션하여, 단 하나의 입구라도 터렛에 의해 영구적으로 막힌다면 터렛 설치를 차단(`canReach = false`)하도록 A* 검증 로직을 즉각 수정 및 보완 완료.
