# Sunken Defense 개발 로그 및 트러블슈팅

프로젝트를 진행하며 날짜별 진행 상황과 발생한 문제(트러블슈팅)를 기록하는 문서입니다.

---

## 2026-08-21
- **진행 내용**: 
  - CraftEngine 분석 및 '성큰 디펜스' 게임 개발 가능 여부 검토 완료.
  - 마우스 입력 처리, 화면 렌더링, A* 알고리즘 등 주요 8가지 기능에 대한 구현 가능 여부 파악.
  - 전체 개발 계획(Implementation Plan) 및 태스크(Task) 문서화.
- **트러블슈팅**: 
  - **(해결) 화면 줌 인/아웃 기능:** 콘솔 화면 특성상 그래픽적 줌이 불가능한 한계가 있었으나, Win32 API의 `SetCurrentConsoleFontEx`를 사용하여 터미널 폰트 크기 자체를 변경하는 방식으로 우회 구현하기로 결정.

---

## 2026-08-24
- **진행 내용**: 
  - `Project_SKDefense` 프로젝트 세팅 및 `DefenseLevel` 메인 레벨로 연동.
  - 50x20 크기의 맵 구조(`SK_Defense_Map.txt`) 설계 및 Wall/Ground 액터 자동 배치 로직 구현.
  - WASD 키를 이용한 자유로운 맵 카메라 이동 구현.
  - 마우스 위치에 2x2 크기 터렛 미리보기(`TT`) 구현 및 왼쪽 클릭 시 터렛 설치 기능 1차 완료.
- **트러블슈팅**: 
  - **(해결) 윈도우 터미널 마우스 실시간 추적 불가 현상:** 최신 터미널 환경에서 마우스를 클릭/드래그하지 않으면 `MOUSE_MOVED` 이벤트를 콘솔 버퍼에 넘겨주지 않는 고질적 문제 발생. 윈도우 OS의 물리적 마우스 픽셀 좌표(`GetCursorPos`)를 윈도우 핸들(`GetConsoleWindow()`) 기준으로 변환한 뒤, 화면 픽셀 비율(`GetClientRect`)로 직접 칸(Cell) 단위 위치를 계산하는 커스텀 추적 함수(`GetRealMousePos()`)를 제작하여 완벽 동기화 달성.
  - **(해결) 마우스 클릭 시 터렛 설치 위치 엇나감:** `GetAsyncKeyState`를 이용한 OS 틱과 엔진 콘솔 큐 간의 이벤트 처리 시차로 인해 좌표가 틀어지는 문제를 방지하기 위해 엔진 자체 이벤트 큐를 유지하되, 마우스 위치만 OS단에서 가져오는 하이브리드 구조를 확립함.

---

## 2026-08-24 (오후)
- **진행 내용**: 
  - A* 길찾기 알고리즘 연동 및 몬스터(`Enemy`) 자동 이동 로직 추가.
  - 마우스 클릭으로 터렛(2x2) 설치 시 지형 데이터를 갱신(장애물 값 `2` 부여)하고, 실시간으로 맵에 생존해 있는 몬스터들의 경로를 재탐색(`RecalculatePath`)하도록 동적 회피 기능 구현.
  - `EnemySpawner` 생성 및 스폰 포인트(`S` 지점) 데이터 동기화 완료.
  - **오브젝트 풀링(Object Pooling) 적용:** `maxPerWave(30)` 만큼의 몬스터를 `BeginPlay` 시점에 미리 메모리에 할당해 두고(`isActive=false`), 스폰 타이머에 맞춰 한 마리씩 재활용(`SetActive(true)`)하여 메모리 할당/해제 오버헤드 대폭 최적화.
  - **웨이브 상태 관리 및 타이머 UI:** `DefenseLevel` 좌측 상단에 웨이브 대기 시간(mm:ss) 및 진행 상태(Wave In Progress)를 렌더링하도록 구현.
- **트러블슈팅**:
  - **(해결) A* 길찾기 알고리즘 구동 시 화면 완전 정지:** `AStar::FindPath` 내부에 디버그를 위한 `DisplayGrid()` 및 `Sleep(50)` 코드가 남아있어 메인 스레드가 정지되는 문제가 발생. 해당 부분을 주석 처리하여 게임 루프 블로킹 현상 해결.
  - **(해결) 몬스터가 S 위치에서 이동하지 않는 버그:** `DefenseLevel::Tick`을 오버라이딩하면서 부모 클래스의 `Level::Tick(deltaTime)` 호출을 누락하여 맵에 소속된 액터들의 `Tick` 루프가 돌지 않던 문제를 발견. 부모 `Tick` 호출 코드를 추가하여 몬스터 등 액터 로직 정상화.
  - **(해결) 새로 지은 터렛을 무시하고 뚫고 지나가는 문제:** A* 탐색 조건에 벽(`1`)만 체크되어 있던 것을 터렛(`2`)도 포함하도록 수정(`grid == 1 || grid == 2`). 터렛 건설 시 맵 갱신 즉시 루프를 돌며 모든 활성화된 몬스터의 `RecalculatePath()` 함수를 강제로 호출하도록 조치.

---

## 2026-08-25
- **진행 내용**:
  - `TurretBullet` 액터 스크립트 작성 및 팩토리(`Project_SKDefense.vcxproj`) 연동 완료.
  - 마우스 실시간 추적 기능과 윈도우 터미널(Windows Terminal)의 가상 콘솔(ConPTY) 아키텍처 간의 호환성 분석 완료.
- **트러블슈팅**:
  - **(해결) TurretBullet 내부에서 FindActor 호출 시 식별 오류:** 총알이 생성된 후 날 쏜 터렛을 `FindActor`로 찾을 경우 다중 터렛 상황에서 식별이 불가능한 논리적 오류 발생. 총알이 생성될 때 발사된 위치(`startPosition`)를 스스로 저장하고, 매 프레임 이동 거리를 계산하여 사거리(`bulletRange`)를 벗어나면 스스로 소멸(`Destroy`)하는 독립적이고 가벼운 구조로 구현.
  - **(해결) 윈도우 터미널 호환 한계:** 윈도우 터미널은 가상 콘솔로 동작하여 물리 좌표 변환(`ScreenToClient`)이 어긋나는 한계가 있어, 안정적인 표준 **콘솔 호스트(cmd)** 환경을 기본 표준으로 채택.

---

## 2026-08-26
- **트러블슈팅**:
  - **(해결) 오브젝트 풀링 재활용 버그:** 웨이브가 넘어갈 때 몬스터가 다시 나오지 않는 현상 발견. 몬스터가 죽거나 아지트에 도달했을 때 `Destroy()`로 파괴해 버려 풀이 고갈되는 문제였음. `SetActive(false)`로 상태만 비활성화하여 풀에 반납하도록 수정.
  - **(해결) 재활용 시 체력 초기화 누락 방지:** 풀에서 깨워낼 때 죽었던 몬스터의 체력이 0 이하로 남아있어 즉시 다시 죽는 현상을 방지하기 위해, `SetActive(true)` 직전에 현재 웨이브 비례 체력(`currentWave * 5`)으로 리셋하도록 보완.

---

## 2026-08-27
- **진행 내용**:
  - **멀티 씬(레벨) 관리자 세팅**: `MainMenuLevel`, `EscMenu`, `GameOverLevel`, `GameClearLevel` 추가 및 연동. ESC 키와 메뉴 간의 씬 전환 흐름 완성.
  - **골드(Gold) 시스템 연동**: 기본 골드(100G) 부여, 몬스터 처치 시 골드 획득(+10G), 터렛 설치 시 골드 차감(-50G) 로직 추가. 골드 부족 시 설치 불가 처리(빨간색 미리보기). 치트키(G: 골드 1000 증가) 추가.
  - **승리/패배 조건 구현**: 아지트 체력 0 이하 시 `GameOverLevel` 전환. 마지막 웨이브 몬스터 전멸 시 `GameClearLevel` 전환. 치트키(F1: 웨이브 대기시간 스킵) 추가.
- **트러블슈팅**:
  - **(해결) 구조체 중복 정의(Redefinition) 오류:** `EscMenu.h`와 `GameOverLevel.h`에서 각각 `MenuItem`을 중복 정의하여 충돌하던 문제를 `MenuItem.h` 공용 헤더로 분리하여 해결.
  - **(해결) ESC 메뉴 진입 시 Out of Bounds 크래시:** `levelList` 배열 크기(2개)와 `enum State`의 인덱스 불일치로 인한 크래시. 모든 씬을 정확한 순서로 `emplace_back`하여 1:1 매칭 완료.
  - **(해결) 웨이브 강제 스킵 시 클리어 불가 버그:** 3웨이브 스폰 직후 `currentWave`가 4로 증가하여 `currentWave == 3` 조건문에 진입하지 못하던 오류를 `currentWave > 3` 상태일 때 전멸 체크하도록 수정.
  - **(해결) 게임 오버 후 Restart 시 상태 잔존 방지:** `RestartDefenseLevel()` 함수를 제작하여 `levelList[1]`에 완전히 새로운 `DefenseLevel` 인스턴스를 할당하도록 구현.

---

## 2026-08-27 (오후)
- **진행 내용**:
  - **터렛 완전 길막(Maze Block) 방지 시스템 완성**:
    - `DefenseLevel::CanBuildTurret`에서 터렛 임시 배치 후 A* 길찾기 시뮬레이션을 수행하여, 스폰 지점(S)부터 아지트(D)까지 경로가 없거나 맵에 살아있는 몬스터가 갇히게 되는 경우 설치를 차단(프리뷰 빨간색 표시 및 클릭 무시).
  - **터렛 철거(판매) 시스템 구현**:
    - 마우스 우클릭 시 해당 위치의 터렛을 철거하고, 타일을 빈 땅(0)으로 복구하며 성급 비례 골드 환불(1성 25G, 2성 75G, 3성 225G) 및 몬스터 경로 동적 재계산 처리.
  - **랜덤 터렛 & 3단 자동 합성(3-Merge Auto Upgrade) 시스템 구현**:
    - 터렛 설치 시 3가지 속성(화염 `FF`, 얼음 `II`, 전기 `TT`) 중 하나가 무작위 생성.
    - 동일 속성/성급(1성/2성) 터렛이 맵에 3개 모이면 가장 먼저 지어진 1번 터렛이 다음 성급(2성 `+`, 3성 `*`)으로 자동 승급하고 나머지 2개는 파괴되며 빈 땅으로 복구.
    - 연쇄 합성 지원: 1성 3개 합성으로 2성이 된 순간 2성이 3개가 되면 즉시 3성으로 연쇄 승급.
- **트러블슈팅**:
  - **(해결) 엔진 RTTI 함수명 충돌:** `GetType()` 함수명이 엔진 매크로 `TYPE_DECLARATIONS`의 RTTI 함수와 충돌하여 컴파일 에러 발생. `GetTurretType()`으로 변경하여 충돌 해결.
  - **(해결) 신규 생성 액터 리스트 탐색 누락:** 설치 직후 프레임에서 새 터렛이 엔진 대기 목록(`addRequestedActorList`)에 머물러 있어 `FindActors`에 누락되는 문제 해결을 위해 두 목록을 모두 수집하여 합성 검사하도록 보완.
  - **(해결) 터렛 희생 시 타일 복구 누락 버그:** 합성 시 파괴되는 2개 터렛의 4칸 타일을 `mapGrid`에서 빈 땅(0)으로 리셋 후 `RecalculatePath()`를 호출하여 유령 벽 버그 원천 차단.

---

## 2026-08-28
- **진행 내용**:
  - **맵 디자인 개편**: 유저가 자유롭게 미로를 설계할 수 있도록 50x20 크기의 2x2 그리드 기반 광장형(Open Field) 맵으로 재설계. 4개 앵커 기둥 배치.
  - **UI 전면 개편**: 화면 우측 여백(X=65~115)에 '전용 UI 패널' 형태로 분리 배치하고 하단 컨트롤 패널은 다중 라인 버튼 레이아웃으로 변경.
  - **Tick 함수 리팩토링**: 거대해진 `DefenseLevel::Tick`을 `HandleCameraInput`, `HandleMouseInput`, `HandleUIInput`으로 기능 단위 분리.
  - **도박 슬롯머신(Gamble) 애니메이션 구현**: 5초간 게임 업데이트를 일시정지하고 중앙 팝업에서 주사위 슬롯머신 애니메이션 연출.
- **트러블슈팅**:
  - **(해결) 카메라 좌표 오차로 인한 맵-UI 오버랩:** 카메라 초기 위치를 `screenWidth / 2`로 재정렬하고 UI 뒷면에 렌더 순위 90의 검정색 공백 배열(Black Cover)을 배치하여 배경 관통 차단.
  - **(해결) ASCII 엔진에서의 멀티바이트(한글) 깨짐:** 엔진 코어 변경 대신 비용 대비 효과가 뛰어난 영문(English) 텍스트 규격으로 전면 전환.
  - **(해결) 팝업 숫자가 증발하는 댕글링 포인터 버그:** `std::to_string().c_str()` 임시 객체 참조로 소멸되던 문제를 `std::string` 로컬 변수 보관 방식으로 안전하게 해결.

---

## 2026-08-31
- **진행 내용**:
  - **GameInfo UI 연동**: `Info/GameInfo.txt` 파싱 및 F12 키 입력 시 화면 중앙에 게임 규칙/조작법 팝업 연출.
  - **F3 디버그 시각화 모드 (A* 길찾기 애니메이션)**: F3 입력 시 몬스터 스폰 속도 지연(10초) 및 터렛 공격 중지로 여유로운 길찾기 관찰 환경 제공. 몬스터 발밑부터 아지트까지 이어지는 선형 경로 애니메이션 구현.
  - **다중 입구 개방 (Multi-Spawn)**: 웨이브 4, 7 도달 시 활성화 입구 개수를 늘려 다중 스폰 지점에서 적이 출현하도록 난이도 스케일링.
- **트러블슈팅**:
  - **(해결) GameInfo 팝업 프레임 저하:** 틱마다 글자 단위로 파일 읽던 코드를 라인 단위 일괄 파싱 및 `Submit`으로 최적화.
  - **(해결) 다중 스폰 지점 변경 후 길막 검증 누락 오류:** 단일 변수 참조 코드 삭제 후 발생한 오류를 모든 스폰 지점 반복 검증으로 완벽 보완.

---

## 2026-09-01
- **진행 내용**:
  - **A* 대각선 모서리 컷(Corner Cut) 방지 (`IsDiagonalBlocked`)**: 대각선 이동 타일의 인접 상/하/좌/우에 벽 또는 터렛이 있을 경우 모서리 뚫기 방지.
  - **F3 디버그 애니메이션 고도화**: 탐색 과정 퍼짐(초록 `+`) 후 최종 경로(마젠타 `*`)가 순차 재생되도록 연출 개편. 몬스터 `sortingOrder`를 85로 올려 가림 방지.

---

## 2026-09-02
- **진행 내용**:
  - **F3 디버그 애니메이션 튜닝**: 탐색 애니메이션이 경로 재계산 시 1회만 재생되고 최종 경로만 남도록 연출 간소화. UI에 ON/OFF 인디케이터 추가.
  - **미로 설계 보호 기능**: F3 디버그 모드 중에는 자동 합성(Merge) 기능을 비활성화하여 3개 합성으로 미로가 뚫리는 현상 방지.
- **트러블슈팅**:
  - **(해결) 아지트 가림 현상:** 아지트 `sortingOrder`를 90으로 상향하여 디버그 경로 위에 뚜렷하게 보이도록 수정.
  - **(해결) 랜덤 업그레이드(R) 이중 차감 버그:** `Tick()`과 `HandleUIInput()` 양쪽에 남아있던 중복 입력 감지 로직을 단일화하여 해결.

---

## 2026-09-03
- **진행 내용**:
  - **`ServerCore` IOCP 네트워크 정적 라이브러리(.lib) 구축**:
    - `IocpCore`, `Listener`, `Session`, `NetAddress` 비동기 I/O 완성.
    - `RecvBuffer` 슬라이딩 윈도우 기반 수신 버퍼로 TCP 패킷 뭉침 및 쪼개짐 완벽 해결.
  - **프로토콜 및 패킷 시스템 설계 (`Common/Protocol.h`, `Common/PacketType.h`)**:
    - 바이트 정렬(`#pragma pack(push, 1)`) 4바이트 헤더(`size`, `id`).
    - 로그인, 채팅, 터렛 건설/판매, 방 정보 패킷 구현.
  - **`SK_Defense_Server` 전용 게임 서버 가동**:
    - 4개 Worker Thread 가동 및 `GameRoom` 기반 멀티 세션 브로드캐스트.
  - **PowerShell 기반 패킷 테스터 (`test_client.ps1`) 개발 완료**.
- **트러블슈팅**:
  - **(해결) `ServerCore.pch` C1083 오류:** 소규모 라이브러리 특성에 맞춰 PCH를 비활성화하여 컴파일 안정성 확보.
  - **(해결) `::accept` 스택 버퍼 오버플로우:** 소켓 변수 주소에 소켓 주소 구조체를 덮어쓰던 오염 버그를 전용 구조체 전달로 수정.
  - **(해결) 수신 완료 시 세션 객체 자폭 크래시(0xC0000005):** `shared_from_this()`로 로컬 스코프 세션 수명 안전하게 보장.

---

## 2026-09-05 (오늘)
- **진행 내용**:
  - **솔루션 전역 빌드 환경 정상화**:
    - VS2022 환경 대응을 위해 `Directory.Build.props`를 도입하여 모든 프로젝트의 툴셋을 `v143`으로 고정하고 `/utf-8` 인코딩을 적용.
    - `Project_SKDefense.vcxproj`에 `..\ServerCore\` 인클루드 경로를 추가하여 `Common/Protocol.h` 참조 정상화.
  - **오프라인 싱글플레이 UI 프리징 버그 해결**:
    - 서버가 켜져 있지 않을 때 매 프레임 동기 `connect()`가 블로킹 호출되어 1초간 화면이 굳는 문제를 1회 접속 시도 플래그로 개선.
  - **메인 메뉴 반응형 중앙 정렬 픽스**:
    - 싱글 모드 안내 문구(`"Press Enter Button."`) 및 로비 텍스트가 고정 좌표 하드코딩으로 중앙에 맞지 않던 문제를 `(screenWidth / 2) - ((int)text.length() / 2)` 공식을 적용하여 완벽하게 정중앙 정렬.
  - **피스메이커(PEACEMAKER) 데몬 소환 위치 오프셋 분석**:
    - 데몬 소환 이펙트 중심점과 실제 소환되는 몬스터 스프라이트/콜라이더의 피벗 좌표 간의 오프셋 구조를 분석하여 보고.
  - **누적 소비 골드(`totalGoldSpent`) 시스템 구현**:
    - `DefenseLevel::SpendGold(int amount)`에서 터렛 건설(50G), 속성 업그레이드(100G), 랜덤 업그레이드(80G), 도박(300G) 등 모든 골드 지출을 실시간 누적 집계.
    - 실시간 소비 골드 보고 패킷 `C_SPEND_GOLD_PACKET`을 추가하여 멀티플레이 시 각 세션의 총 소비 골드를 서버에 실시간 동기화.
  - **게임 클리어 화면(`GameClearLevel`) 전면 개편**:
    - 콘솔 해상도 기준 가로/세로 완전 정중앙 반응형 레이아웃 구성.
    - 화려한 황색/청색 게임 클리어 축하 헤더 배너 출력.
    - **싱글플레이 모드**: 플레이어가 소비한 총 골드 표시 및 지출 성향에 따른 3단계 칭호(Master Tactician / Veteran Commander / Tycoon Defender) 출력.
    - **멀티플레이 모드**: 방에 참여한 모든 플레이어의 성적표 테이블 출력. 가장 많은 골드를 소비한 유저에게 **1등 및 `MVP ★` 칭호 부여**, 본인 플레이어 `(YOU)` 하이라이트.
    - `Restart Game` (싱글은 즉시 재시작, 멀티는 대기실 로비 복귀) 및 `Quit to Main Menu` 키보드 인터랙션 메뉴 연동.
  - **즉시 클리어 치트(`VK_F2`) 서버 동기화 구현**:
    - 단축키 F2 입력 시 로컬 화면만 넘어가는 대신 `C_GAME_CLEAR_PACKET(totalGoldSpent)`를 서버로 전송.
    - 서버 `GameRoom`에서 방 상태를 `WAITING`으로 전환(웨이브 정지)하고 전원 소비 골드를 수집 및 정렬하여 `S_GAME_CLEAR_PACKET`을 방 전체에 브로드캐스트.
    - 방 안의 모든 클라이언트가 동일한 시점에 게임 클리어 화면으로 동시 전환되며 상호 랭킹 확인 가능.
  - **다중 게임 룸 관리자(`GameRoomManager`) 및 동적 룸 배정 아키텍처 구축**:
    - 기존 전역 단일 룸(`GGameRoom`) 고정 구조를 다중 룸 관리자(`GameRoomManager`)로 전면 개편.
    - 클라이언트 접속(`C_LOGIN`) 시 대기 중(`WAITING`)이면서 여유 정원(최대 2인)이 있는 방을 탐색하여 자동 배정하고, 없거나 기존 방이 모두 게임 진행 중(`PLAYING`)이면 고유 ID를 부여한 신규 `GameRoom`을 동적 생성.
    - `GameSession`에 `std::weak_ptr<GameRoom>`을 설정하여 순환 참조 메모리 누수를 원천 차단하고, `ClientPacketHandler`가 세션이 속한 방으로 모든 패킷을 독립 라우팅.
    - 서버 메인 루프에서 모든 활성 방의 몬스터 스폰 타이머(`room->Update`)를 독립 틱으로 병렬 처리하고 유저가 없는 빈 방은 자동 회수.
  - **전체 솔루션 무결성 검증**:
    - `ConsoleGameProject.slnx` 솔루션 내 7개 프로젝트(`CraftEngine`, `ServerCore`, `Game`, `PEACEMAKER`, `Project_SKDefense`, `ShootingGame`, `SK_Defense_Server`) 모두 MSBuild 검증 결과 **오류 0개, 경고 0개**로 빌드 완료.
- **트러블슈팅**:
  - **(해결) `WS2tcpip.h` IPPROTO_IPV6 및 소켓 심볼 충돌 오류:** `GameClearLevel.h`에서 `Protocol.h`를 직접 인클루드하면서 `WinSock2.h`보다 먼저 `Windows.h`가 로드되어 Winsock 1.1과 Winsock 2 충돌 발생. `GameClearLevel.h`에서는 구조체를 전방 선언(`struct ClearRecord;`)하고 `.cpp` 파일에서만 `WinSock2.h`를 선두로 포함하도록 격리하여 충돌을 원천 차단함.
  - **(해결) ServerCore 빌드 후 헤더 동기화 타이밍 이슈:** `ServerCore` 수정 시 post-build 이벤트를 통해 `Includes/` 폴더로 헤더가 복사되는데, 종속 프로젝트들이 빌드되기 전 최신 헤더가 반영되도록 빌드 순서와 인클루드 경로를 안정화함.
  - **(해결) 대기실(로비) 실시간 접속자 수 미갱신 버그:** 새 플레이어가 방에 접속할 때 `GameRoom::Enter`에서 `BroadcastRoomInfo()`를 호출하지 않아 다른 클라이언트들의 플레이어 수가 레디를 누르기 전까지 `1 / 2`로 고정되던 문제를 발견. 세션 추가 즉시 `BroadcastRoomInfo()`를 실행하여 모든 클라이언트가 실시간으로 `Players: 2 / 2` 및 레디 상태를 즉각 전달받도록 수정.
  - **(해결) 2인 멀티플레이어 동시 시작 시 크래시 튕김 버그:** 
    1. `DefenseLevel::CanBuildTurret`의 A* 길찾기 유효성 검사에서 `AStar::FindPath`가 이전 호출의 `openList`와 `closedList` 노드를 정리하지 않고 누적하여 힙 손상(Heap Corruption) 및 댕글링 포인터 해제 크래시 발생 -> `AStar::FindPath` 진입 시 `openList`와 `closedList`를 완전 정리하도록 수정.
    2. 레벨 전환 프레임 시차로 인한 렌더링 가드 부재 -> `DefenseLevel::Draw()` 초입에 `if (mapGrid.empty()) return;` 가드를 추가하여 초기화 완료 전 미완성 맵 렌더링으로 인한 크래시 방지.
  - **(해결) 솔로 플레이 진행 중 타 클라이언트 2인 멀티플레이 시작 불가 버그:**
    - **원인**: 1번 클라이언트가 엔터 2회로 단독 시작(`forceStart = true`)하여 방 상태가 `PLAYING`이 된 후, 2번/3번 클라이언트가 접속하여 엔터를 칠 때 기존 `GameRoom::HandleReady`가 `if (state == RoomState::PLAYING) return;`으로 패킷을 전면 무시하였고, 세션 리스트에 3명이 모두 묶여 있어 `readyPlayerIds >= sessions.size()`(2 >= 3) 조건이 영원히 충족되지 않아 대기실에서 무한 대기하는 현상이 발생함.
    - **해결**: `GameRoomManager`를 도입하여 이미 `PLAYING` 중인 1번 유저의 방과 별개로, 2번/3번 유저에게 새로운 대기실 룸을 배정하여 독립된 2인 멀티플레이 게임이 정상적으로 시작되도록 완벽 분리.
  - **(해결) 방 퇴장 시 잔여 플레이어 정보 갱신 누락 및 조기 WAITING 리셋 문제:**
    - 기존 `GameRoom::Leave`에서 `if (sessions.size() <= 1) return;`으로 인해 2인 플레이 중 1명이 퇴장할 때 남은 1명에게 인원수 갱신 브로드캐스트가 누락되고 게임 상태가 초기화되던 버그를 `sessions.empty()` 체크로 개선하고, 잔여 플레이어에게 퇴장 알림 및 갱신된 방 인원수 패킷을 정상 전송하도록 수정.

