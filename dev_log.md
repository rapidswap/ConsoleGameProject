# Sunken Defense 개발 로그 및 트러블슈팅

프로젝트를 진행하며 날짜별 진행 상황과 발생한 문제(트러블슈팅)를 기록하는 문서입니다.

## 2026-08-21
- **진행 내용**: 
  - CraftEngine 분석 및 '성큰 디펜스' 게임 개발 가능 여부 검토 완료.
  - 마우스 입력 처리, 화면 렌더링, A* 알고리즘 등 주요 8가지 기능에 대한 구현 가능 여부 파악.
  - 전체 개발 계획(Implementation Plan) 및 태스크(Task) 문서화.
- **트러블슈팅**: 
  - (해결) *화면 줌 인/아웃 기능:* 콘솔 화면 특성상 그래픽적 줌이 불가능한 한계가 있었으나, Win32 API의 `SetCurrentConsoleFontEx`를 사용하여 터미널 폰트 크기 자체를 변경하는 방식으로 우회 구현하기로 결정.

## 2026-08-28 (Map, UI 개편 및 도박 슬롯머신 애니메이션 구현)
- **진행 내용**:
  - **맵 디자인 변경**: 유저가 자유롭게 길막(Mazing)을 설계할 수 있도록 50x20 크기의 2x2 그리드 기반 광장형(Open Field) 맵으로 재설계. 4개의 앵커(기둥) 배치.
  - **인터페이스(UI) 전면 개편**: 기존 맵 중앙에 겹쳐서 출력되던 텍스트들을 화면 우측 여백(X=65~115)에 '전용 UI 패널' 형태로 분리 배치. 하단 컨트롤 패널은 시각적 인지가 빠른 다중 라인(`Multi-line`) 버튼 레이아웃으로 변경.
  - **Tick 함수 리팩토링(Refactoring)**: 점점 거대해지는 `DefenseLevel::Tick` 함수를 `HandleCameraInput`, `HandleMouseInput`, `HandleUIInput`으로 기능 단위로 분리하여 유지보수성 대폭 향상.
  - **랜덤 기능(Upgrade/Gamble) 구현**: 상태 머신(State Machine) 패턴을 적용하여, 슬롯머신(Gamble) 애니메이션이 재생되는 5초 동안 본 게임의 Update(`Level::Tick`)를 스킵(일시정지)하고 중앙 팝업을 띄우도록 연출.

- **트러블슈팅**:
  - **(해결) 카메라 좌표계 오차로 인한 맵-UI 오버랩 버그**: 카메라 초기값이 맵의 정중앙으로 잡혀 있어 맵 우측 절반이 UI 뒤에 숨겨지고 마우스 클릭 좌표가 어긋나는 문제 발생. 카메라의 초기 위치를 화면 절반(`screenWidth / 2`)으로 재정렬하여 맵(X=0~49)과 UI(X=65~115)를 시각적으로 분리함. 또한 UI 뒷면에 맵 오브젝트들이 관통되어 보이는 현상을 막기 위해 렌더 순위(90)에 검정색 공백 배열(Black Cover)을 배치하여 배경을 차단함.
  - **(해결) ASCII 기반 콘솔 엔진에서의 멀티바이트(한글) 깨짐 현상**: `CraftEngine`은 글자를 1바이트(`CHAR_INFO`)로 계산하여 렌더링하지만 한글은 2~3바이트이므로 문자가 깨지고 배열이 틀어지는 증상 발생. 엔진 코어의 유니코드(`wchar_t`) 전환 대공사 대신, 비용 대비 효율이 높은 영문(English) 텍스트 교체 방식을 채택하여 빠르고 세련되게 버그를 픽스함.
  - **(해결) 팝업 숫자가 증발하는 댕글링 포인터(Dangling Pointer) 버그**: 주사위 숫자를 그릴 때 `std::to_string(num).c_str()`과 같이 임시 문자열 객체의 메모리 주소를 바로 참조하여, 렌더링 직전에 객체가 소멸하면서 글자가 그려지지 않는 전형적인 C++ 메모리 함정에 빠짐. `std::string` 로컬 변수를 선언해 데이터를 안전하게 연장 보관한 후 참조하도록 수정하여 해결함.
  - **(참고) 모듈러 편중(Modulo Bias) 현상 리뷰**: `rand() % 3` 등을 사용할 때 발생하는 미세한 확률적 편중성(약 0.003%)에 대해 검토했으나, 캐주얼 디펜스 게임에서 유저 체감이 불가능한 수준이므로 C++11 `<random>` 라이브러리의 오버헤드 대신 기본 알고리즘을 유지하기로 기획 방향 확정.

## 2026-09-03 (IOCP 고성능 네트워크 엔진 & 멀티플레이어 디펜스 서버 구현)
- **진행 내용**:
  - **`ServerCore` 정적 라이브러리(.lib) 구축**:
    - `Type.h` / `CoreMacro.h`: 고정 크기 정수(`int32_t`, `uint16_t` 등) 및 `ASSERT_CRASH` 매크로 정의.
    - `IocpCore` / `IocpEvent`: Windows IOCP 완료 포트 및 이벤트 객체 추상화.
    - `NetAddress`: IPv4 주소/포트 유틸리티 래퍼.
    - `Listener`: 비동기 클라이언트 수락(Accept) 스레드 루프 구현.
    - `Session`: 비동기 송수신(`WSASend`/`WSARecv`) 및 연결 생명주기 관리.
    - `RecvBuffer`: 슬라이딩 윈도우 커서(`readPos`, `writePos`) 기반 수신 버퍼 구현으로 **TCP 패킷 뭉침(Coalescing) 및 패킷 쪼개짐(Fragmentation) 완벽 해결**.
  - **프로토콜 및 패킷 시스템 설계 (`Common/Protocol.h`, `Common/PacketType.h`)**:
    - 모든 패킷에 4바이트 헤더(`size`, `id`) 강제 정렬 (`#pragma pack(push, 1)`).
    - 게임 전용 패킷 정의:
      - `C_LOGIN_PACKET` / `S_LOGIN_OK_PACKET` (플레이어 접속 및 고유 ID/골드 발급)
      - `C_CHAT_PACKET` / `S_CHAT_PACKET` (실시간 채팅)
      - `C_BUILD_TURRET_PACKET` / `S_BUILD_TURRET_PACKET` (타워 설치 및 실시간 좌표/잔여 골드 동기화)
  - **`SK_Defense_Server` 전용 게임 서버 애플리케이션 가동**:
    - 4개의 Worker Thread를 가동하여 IOCP 일감 분산 처리.
    - `GameSession` 클래스 분리 및 이벤트 토스 구조 확립.
    - `ClientPacketHandler` (패킷 교통정리소): 도착한 패킷 ID를 역직렬화하여 세부 핸들러로 분기 처리.
    - **`GameRoom` (게임 방) 및 실시간 `Broadcast` (전체 방송) 시스템 구현**:
      - 다중 플레이어 세션 목록 관리 및 접속 시 고유 ID 부여.
      - 타워 건설(`C_BUILD_TURRET`), 채팅(`C_CHAT`), 방 입장/퇴장 이벤트를 방 안의 모든 플레이어에게 실시간 동기화 완료!
  - **테스트 환경 구축 (`test_client.ps1`)**:
    - PowerShell 기반의 실시간 대화형 패킷 테스터(C# 기반 비동기 백그라운드 리시버 탑재)를 개발하여 2인 이상 동시 접속 및 실시간 멀티플레이 패킷 브로드캐스트 검증 완료.

- **트러블슈팅**:
  - **(해결) `ServerCore.pch`를 찾을 수 없음 (C1083) 및 컴파일 오류 지옥**:
    - *원인*: 파일 이동 및 프로젝트 설정 변경 과정에서 `pch.cpp`의 `<PrecompiledHeader>Create` 속성이 누락되어 PCH 파일이 생성되지 않음.
    - *해결*: 파일 수가 적은 라이브러리 환경에서 빌드 시간 차이가 거의 없고 설정 오류만 유발하는 PCH를 `<PrecompiledHeader>NotUsing</PrecompiledHeader>`으로 완전 해제하여 컴파일 안정성 확보.
  - **(해결) `::accept` 호출 시 스택 버퍼 오버플로우 메모리 오염 버그**:
    - *원인*: `::accept(..., reinterpret_cast<SOCKADDR*>(&clientSocket), ...)` 형태로 8바이트 정수 변수 주소에 16바이트 구조체 데이터를 덮어써서 스택과 소켓 변수가 동시에 오염되어 즉시 크래시 발생.
    - *해결*: 주소 전용 구조체 `&clientAddr`로 인자를 정상 전달하여 메모리 오염 원천 차단.
  - **(해결) 비동기 수신 완료 시 세션 객체 자폭(Destroy) 크래시 (0xC0000005)**:
    - *원인*: 수신 완료 시 `recvEvent.owner = nullptr;`을 실행하는 순간, 외부에서 세션을 잡고 있지 않아 레퍼런스 카운트가 0이 되며 `Session` 객체가 스스로 소멸(Delete)됨. 그 상태에서 후속 코드를 실행하여 댕글링 포인터 Access Violation 발생.
    - *해결*: `ProcessRecv` 진입 시 `auto self = shared_from_this();`로 로컬 스코프에서 세션 수명을 안전하게 락업(Lock-up)하여 해결.
  - **(해결) C++ 바이트 역참조(`*reinterpret_cast<T*>`) 이중 포인터 오인**:
    - *원인*: 바이트 포인터(`BYTE*`)를 구조체 포인터로 캐스팅한 후, 함수의 참조형 매개변수(`T&`)로 전달하기 위해 전치 역참조 연산자 `*`를 사용함.
    - *해결*: 포인터 타입 변환과 역참조의 동작 원리를 명확히 분석 및 문서화.
